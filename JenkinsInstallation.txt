Installing Jenkins with WAR file
NB: Install Java JRE or JDK if not installed.
1.“java -jar jenkins.war”
2.Type in “localhost:8080” on browser to open Jenkins
3. Input password that is on the terminal provided by the installation of Java to continue installation
4. Install suggested plugins 
5. Create first admin user
6. Leave Instance Configuration as Default